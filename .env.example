# SnapCheck Production Configuration Template
# 
# Скопируйте этот файл в .env и заполните реальными значениями
# НИКОГДА не коммитьте .env файл с реальными секретами в Git!

# =============================================================================
# СЕКРЕТЫ (ОБЯЗАТЕЛЬНО ИЗМЕНИТЬ!)
# =============================================================================

# JWT Secret Key - используется для подписи токенов
# Генерация: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-min-32-chars-CHANGE-THIS

# База данных PostgreSQL
POSTGRES_USER=snapcheck_user
POSTGRES_PASSWORD=your-secure-password-CHANGE-THIS
POSTGRES_DB=snapcheck
DATABASE_URL=postgresql://snapcheck_user:your-secure-password-CHANGE-THIS@db:5432/snapcheck

# =============================================================================
# ДОМЕН И ОКРУЖЕНИЕ
# =============================================================================

DOMAIN=lms.it-uae.com
ENVIRONMENT=production
ALLOWED_ORIGINS=https://lms.it-uae.com

# =============================================================================
# EMAIL ДЛЯ LET'S ENCRYPT
# =============================================================================

ACME_EMAIL=admin@lms.it-uae.com

# =============================================================================
# ОПЦИОНАЛЬНЫЕ НАСТРОЙКИ
# =============================================================================

# Backend
WORKERS=4
LOG_LEVEL=info
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Frontend
NODE_ENV=production

# =============================================================================
# ПРИМЕЧАНИЯ
# =============================================================================

# 1. Генерация SECRET_KEY:
#    python3 -c "import secrets; print(secrets.token_urlsafe(32))"
#
# 2. Генерация сложного пароля:
#    python3 -c "import secrets; print(secrets.token_urlsafe(24))"
#
# 3. Проверка .env файла:
#    cat .env | grep -v "^#" | grep -v "^$"
#
# 4. Права доступа к .env:
#    chmod 600 .env
#
# 5. Убедитесь что .env добавлен в .gitignore!
